# 贪心算法的思考-梯度下降步长与方向的工程优化设计（B方案）

## 前言

本设计的出发点是优化机器学习中贪心算法以及梯度下降的工程学角度的弊端，进行的个人思考。其中涉及的思路并不是创新的，因为很早就有人做过。受限于个人实验等事项的问题，在设计思路的时候并没有查阅是否有人已经做过。一来是时间原因，二来是我喜欢独立思考独立推导，创新性与否并不是我最关注的，我最关注的是我要自己通过学习机器学习，自己设计一套思路。这就是我的学习习惯，我会独立推导一些方法，推导完后再看是否有人做过，有的话就进一步优化自己的思维，锻炼自己的独立性。

## 问题分析及解决

从以下问题的基础上解决这一问题：

1. 梯度方向的本质与工程局限性

LOSS函数控制梯度下降，同时LOSS的梯度下降如果小到一定阈值就停止下降，这是一般而言的ML思路。这本质属于贪心算法，每次下降都找此点下降最大的方向。这本质是有问题的，因为梯度反映的是此点向周围位移极小的极限距离（→0）的时候下降最大的方向，并不等价于下降一定向量长度的方向依然是此方向。当然，这是工程学问题，而且是一个普遍接受的办法。但是这显然造成了并不一定一致的问题。

2. 贪心算法的局部最优问题

每次都选择此次基础上下降量最大的点作为下一个起始点，并不一定代表一定是最终下降量最大。因为存在一些类似我们化学上讲的‘能垒’问题，某个点并不一定是此集合中下降量最小的，但如果选择它，那么在它基础上出现的集合存在一个下降量极其大的点。所以贪心算法是有问题的。蒙特卡洛方法本质就是为了规避贪心算法的弊端。我系统了解过AMDEN模型以及ATAT模型并做过深入理解和操作，他们都通过MC方法规避贪心算法的问题。

## 思路概述

备注：‘能垒’是指按照某个方向一开始下降并非最大，但是越过后下降很大，类似化学里的过程-能量图。用能垒代指这种过程。
A方案：设计一套拉齐算法解决贪心算法的弊端。后来发现已经有人做过类似的，以及拉齐算法需要遍历大量点，所以思考了新的思路；
B方案：通过算某点的梯度下降方向-步长长度后的点以及其他方向步长长度后的点的下降情况，来确定是否采纳某个梯度下降的结论。

## 思路详解

A方案：愚公移山
对于一神经网络：先正常跑 等到最后停止，停止的依据是最终LOSS不怎么下降了。这时候就：从此时LOSS不再下降的点，向周围寻找附近的LOSS点 如果比这个点大 就把他们数值赋值为LOSS不再下降的点。如果发现更低的 那么这个更低的 和后续发现的比他更高的点统一拉齐为这个点值。这样探索全局。就能把复杂的函数优化为阶梯函数了。此时，从最开始LOSS停止梯度下降的点到最终最小点之间拉一条线，按这条线强制更新LOSS 这样就把贪心算法变成了全局优化 简称‘愚公移山’
后续发现这个策略需要大量点，成本巨大。以及存在类似的填充法，所以这个思路作为我的思考心路保留，但是确实我承认其中的弊端，所以我独立思考了其他方法：
为了防止局域最小值 是不是可以分多条路下山。一条是贪心算法 另n条是强制指定方向。然后达到各自最小后随机向周围几个点求值 看看有没有更小的
这套思路写完后也发现工程上很多人在做。但这没什么，因为重要的是独立思考能力的锻炼。我能独立想到这，说明我的能力很充足。
B方案：六方向多步长扫描
程序设定的下降步长为x，注意，此处的步长是指坐标轴的步长而非常规工程的直接作用于W和B的步长，这时候就可以结合坐标的定义去反解W，b的真实下降步长了。通过算某点的梯度下降方向，方向记为a-按照a方向移动并保证底面坐标变化值长度为x，以及其他五个方向b，c，d，e，f，要求这五个方向以及方向a在底面的投影相邻两两间是60度，并分析移动相同坐标轴底面变化量后他们带入的LOSS数值，通过他们来确定是否采纳这个梯度下降的结论，择最优者。
同时每次多找几个更小的底面坐标轴移动步长，记为x-v，x-b，x-n，每个步长都通过a-f六个方向动，求各自LOSS。多步长结论统一比较，如果发现某个小步长LOSS下降最大，就把他标记起来，然后实际下降还是按最大步长x的最大下降方向下降（不用小步长是为了防止掉入极小值山谷）。这样最终就标记了多个极小值点，最终把所有标记的和LOSS走的比较，选最小的，作为最终LOSS点。
同时极小值能用于分析特征和极小值的关系。为什么存在这个极小值点？这个极小值点下各种特征有什么分布，或者什么彼此的关系？就可以探讨了。
同时，也对小步长做方向调整的下降，标记最小值点，如果存在。方向调整本身就是反对贪心算法的。最终把所有点全部连起来图示化展示，便于观察LOSS的更具体的图像；同时把所有不同方向和的最小值点之间连起来，让LOSS强行按照这个大致方向（允许稍微偏离。同时还要结合梯度下降的一般工程逻辑）重跑一次，这时候观察是否能找到更小的LOSS。因为之前LOSS停止的标准是大步长的结论，没考虑小步长还可以下降。这次是小步长+考虑能垒的结论。我们的假设是存在‘能垒’且能垒路径有可能更快。
为什么这也是对贪心算法的优化？因为不同方向的最小值点如果存在，那么它天然是反梯度下降方向的，本身就可能是一种这个方向有‘能垒’的代表，它更可能趋近于全局最优路径。当然需要结合实验验证可以更说明这个结论。

##诚实性&创新性说明
六方向六边形采样、多步长比较、归一化坐标步长等组件在已有文献中均有出现（信赖域方法、线搜索、Aquila Optimizer等）。本工作的价值在于：
完整组合：六方向×多步长扫描 + 极小值标记 + 能垒路径重跑 + loss地形图可视化，整套组合未发现完全相似工作；
跨领域应用：极小值点用于分析催化剂体系特征指纹，不同极小值=不同体系解释，这个跨领域连接未发现相似工作；
可证伪设计：通过大步长贪心 vs 小步长+能垒路径两次重跑对比，验证能垒假设。
优化了梯度下降的步长和方向的工程角度的问题。

## 辩论问题说明

此处仅给出算法思路，实验证明和严谨性验证部分后续补充。我的时间太紧张了。我也没办法。而且回到最初的话，最重要的是有思路，有思路才能有机会验证。

## 跨领域应用：极小值即体系指纹

本优化器完全独立可使用（只依赖numpy，可优化任何loss函数）。在催化剂/材料等跨领域场景中，极小值点具有特殊价值：
- 每个极小值点对应一组参数权重组合，代表一种可能的"体系解释"
- 通过分析极小值点的主导特征，可以探讨"为什么存在这个极小值点？这个极小值点下各种特征有什么分布或关系？"

与传统优化器对比：
| 传统梯度下降/Adam | 本B方案优化器 |
|---|---|
| 直接α×gradient更新参数 | 固定坐标步长x，六方向扫描选最优方向 |
| 只输出最终参数 | 输出参数 + 所有采样点 + 极小值点 + loss地形图 |
| 只为收敛 | 既收敛又生成多极小值分析数据 |
| 黑盒过程 | 全程可可视化，可解释 |

## 结尾-想说的话

本工作是个人在材料领域学习实践中的思考性工作。更多作为思路展示。
本设计大部分思路是自己想的，后续搜索文献发现才知道其中的很多部分也有前人做过。对于B方案，目前我没发现整套思路高度相似/完全一致的。
本设计思路全程架构由自己完成，写这个WORD是方便非ML科班出身的同志观看。
如果没有时间比我更早（我的时间标志是Github发布时间）的发布，则后续发现完全撞车或者高度相似的工作务必引用并说明这是我的成果。
如果大家发现思路设计有其他需要讨论的问题，或者存在高度相似/完全撞车的早于我的全流程工作欢迎大家留言，我看到后将及时补充说明。
思路方面没问题，代码方面的问题后续将进一步核查。如有代码错误欢迎指正。
